# dependencies (bun install)
node_modules

# output (keep .gitkeep so go:embed always has a dist/ to embed)
dist/*
!dist/.gitkeep
*.tgz

# code coverage
coverage
*.lcov

# caches
.cache
*.tsbuildinfo

# dotenv environment variable files
.env
.env.local

# Finder (MacOS) folder config
.DS_Store
.env.development
