# Copy to .env.development — where the lovr API (go run ./lovr web) is
# listening during development. Production builds run with NODE_ENV=production
# and intentionally do not load it: the embedded UI is same-origin.
BUN_PUBLIC_API_URL=http://127.0.0.1:8080
